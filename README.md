# E. coli Keio Knockout Growth Curve Atlas

Interactive visualization of ~94 *E. coli* knockout strains grown in LB and M63 media,
clustered with [KinBiont.jl](https://github.com/pinheiroGroup/KinBiont.jl).

**Data source**: [Nature Scientific Data 2026](https://www.nature.com/articles/s41597-026-07075-9)

## Live site

Served from `docs/` via GitHub Pages.

## Setup

```bash
# 1. Install Julia dependencies and dev KinBiont
julia --project=analysis -e '
  using Pkg
  Pkg.develop(path="../KinBiont.jl")   # adjust path if needed
  Pkg.instantiate()
'

# 2. Run the analysis (generates docs/data/curves_data.json)
julia --project=analysis analysis/analyse.jl

# 3. Preview locally
python3 -m http.server 8080 --directory docs
# open http://localhost:8080
```

## GitHub Pages

In the repo settings → Pages → Source: **Deploy from branch `main`, folder `/docs`**.

Commit `docs/data/curves_data.json` along with `docs/index.html`.

## Project structure

```
curves-data/
├── *.xlsx                   # Raw data files (from the paper)
├── analysis/
│   ├── Project.toml         # Julia environment
│   └── analyse.jl           # Analysis script → outputs JSON
└── docs/
    ├── index.html           # Plotly visualization (GitHub Pages entry)
    └── data/
        └── curves_data.json # Generated by analyse.jl (commit this)
```

## What the analysis does

1. Reads `Curves_knockouts_media.xlsx` to map each curve to a gene and medium
2. Loads raw OD time series from `Growth_curves_LB.xlsx` and `Growth_curves_M63.xlsx`
3. Averages replicates per (gene, medium) → ~94 × 2 curves
4. Sweeps k = 2…10 with KinBiont's k-means + WCSS to find the elbow
5. Clusters gene-level curves separately for LB and M63
6. Exports times, centroids, cluster assignments, WCSS sweep to JSON

## Visualization tabs

| Tab | Description |
|-----|-------------|
| **Overview** | Cluster size bars + LB→M63 phenotype shift heatmap |
| **Elbow Analysis** | WCSS vs k plot with optimal k marked |
| **Cluster Profiles** | Mean OD curves per cluster ± 1 SD |
| **Gene Browser** | All gene curves coloured by cluster, searchable table |
| **LB vs M63** | Per-gene overlay of both conditions with SD bands |
